# A statically generated blog using Next.js, TypeScript, MDXv2, Lunr, Tailwind and Next-Auth

Commands:

- npm run dev
- npm run build && run start

- npm run lunr (will create an lunrIndex.json at the root)
